name: Test

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: "test"
        run: |
         set -eo pipefail
         set +x
         ./test.sh
